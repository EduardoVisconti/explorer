// for .. of

let name = 'Eduardo'

for (let char of name) { //pega cada caracter do nome
  console.log(char)
}


let names = ['João', 'Junin', 'Gab']

for (let name of names) { //pega cada nome que está no Array
  console.log(name)
}